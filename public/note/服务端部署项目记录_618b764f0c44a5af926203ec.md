```
部署项目步骤
后台代码自动上传/opt/blog-server
前端代码打包后dist 放入后台/public/
启动后台服务 pm2 start ./bin/www --watch
启动nginx服务 systemctl start nginx.service
```

### 一、服务器

```
host: 192.168.101.102
username: root
password: rousean
```

### 二、Nginx

```
安装目录 	  -->> 	  /etc/nginx
配置文件      -->> 	  /etc/nginx/nginx.conf
启动nginx  	-->> 	systemctl start nginx.service
停止nginx  	-->> 	systemctl stop nginx.service
重启nginx  	-->> 	systemctl restart nginx.service
重新加载配置   -->> 	 systemctl reload nginx.service
```

### 三、Mysql

```
安装目录  		-->>    /usr/local/mysql
启动mysql		  -->>    systemctl start mysqld
停止mysql	      -->>    systemctl stop mysqld
查看mysql	      -->>    systemctl status mysqld
username: root
password: rousean
```

### 四、MongoDB

```
安装目录	-->>   /usr/local/mongodb
启动mongo   -->>   systemctl start mongodb.service
启动mongo   -->>   systemctl stop mongodb.service
启动mongo   -->>   systemctl status mongodb.service
url: 192.168.101.102:27017
```

###	五、Node

```
安装目录  --> /opt/node
全局命令 --> node、npm、cnpm、pm2
后台服务启动  --> pm2 start ./bin/www --watch
停止node服务 根据项目ID停止  -->   pm2 stop 0
停止node服务 根据名称停止    -->   pm2 stop www
停止node服务 根据js文件停止  -->   pm2 stop ./bin/www
```

### 六、VSCode 工具

```
sftp 将本地项目上传远程服务器
ssh  连接远程服务器
```

